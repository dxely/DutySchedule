pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradPluginPortal()
    }
}
dependencyResolutionManagement {
    repositoryMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "DutySchedule"
include ':app'
